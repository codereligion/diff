<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.codereligion</groupId>
    <artifactId>codereligion-diff</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>Diff</name>

	<description>Allows to diff two arbitrary Java objects and retrieve the diff result as a list of strings.</description>

	<developers>
		<developer>
			<id>SierraGolf</id>
			<name>Sebastian Gröbler</name>
		</developer>
	</developers>
	
	<prerequisites>
		<maven>3.0</maven>
	</prerequisites>

	<properties>
        <!-- basic maven properties -->
        <java.version>1.6</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <compiler.plugin.version>3.0</compiler.plugin.version>
        <war.plugin.version>2.3</war.plugin.version>
        <release.plugin.version>2.4</release.plugin.version>

        <!-- dependency versions -->
        <slf4j.version>1.6.4</slf4j.version>
        <logback.version>0.9.29</logback.version>
		<scala.version>2.9.2</scala.version>
		<diffutils.version>1.2</diffutils.version>
		<mockito.version>1.9.5</mockito.version>
        <junit.version>4.11</junit.version>
        <hamcrest.version>1.3</hamcrest.version>

    </properties>

    <dependencies>

        <!-- ******************** -->
        <!-- LOGGING DEPENDENCIES -->
        <!-- ******************** -->

        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <!-- ******************* -->
        <!-- TESTING DEPENDENCIES -->
        <!-- ******************* -->

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.hamcrest</groupId>
                    <artifactId>hamcrest-all</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.hamcrest</groupId>
            <artifactId>hamcrest-integration</artifactId>
            <version>${hamcrest.version}</version>
            <scope>test</scope>
        </dependency>

        <!-- ******************** -->
        <!-- MISC DEPENDENCIES -->
        <!-- ******************** -->

        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>11.0.1</version>
        </dependency>

		<dependency>
			<groupId>com.googlecode.java-diff-utils</groupId>
			<artifactId>diffutils</artifactId>
			<version>${diffutils.version}</version>
		</dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-release-plugin</artifactId>
                <version>${release.plugin.version}</version>
            </plugin>

            <!-- to assure that maven compile the project with the desired version -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <showWarnings>true</showWarnings>
                    <!-- this is a hack, because <proc:none /> is not a valid tag -->
                    <compilerArgument>-proc:none</compilerArgument>
                    <compilerArguments>
                        <Werror />
                        <Xlint />
                    </compilerArguments>
                </configuration>
            </plugin>


            <!-- skip default execution of surefire -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.12.2</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <profiles>

        <!-- ********* -->
        <!-- UNIT TEST -->
        <!-- ********* -->

        <!-- this profile is ment to be executed before each commit to assure that
            the application passes all basic unit tests. example call: mvn clean verify
            -Dunit.test you can skip code coverage analysis by specifying the property
            "skip.code.coverage" -->
        <profile>
            <id>unit.test</id>

            <activation>
                <property>
                    <name>unit.test</name>
                </property>
            </activation>

            <build>
                <plugins>
                    <!-- execute actual unit tests -->
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <skip>false</skip>
                            <systemPropertyVariables>
                                <logs.folder>${project.build.directory}/logs/test</logs.folder>
                            </systemPropertyVariables>
                        </configuration>
                        <executions>
                            <execution>
                                <id>run.unit.tests</id>
                                <goals>
                                    <goal>test</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
